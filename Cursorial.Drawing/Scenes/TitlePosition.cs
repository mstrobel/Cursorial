namespace Cursorial.Drawing;

/// <summary>Where a panel title sits along the top border edge.</summary>
public enum TitlePosition
{
    /// <summary>Near the left corner (after a short line run). The default.</summary>
    Left,

    /// <summary>Centered along the top edge.</summary>
    Center,

    /// <summary>Near the right corner.</summary>
    Right,
}
