namespace Cursorial.Gallery.Pages;

// The base type (UserControl) comes from the generated half of the partial (the XAML root
// element), so changing the root element in Views.xaml needs no edit here.
public partial class ChartsView
{
    public ChartsView() => InitializeComponent();
}
