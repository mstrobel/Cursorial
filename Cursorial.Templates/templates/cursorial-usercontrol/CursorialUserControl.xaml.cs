namespace CursorialApp.Views;

// The base type (UserControl) comes from the generated half of the partial (the XAML root
// element), so changing the root element in CursorialUserControl.xaml needs no edit here.
public partial class CursorialUserControl
{
    public CursorialUserControl() => InitializeComponent();
}
