<Project Sdk="Microsoft.NET.Sdk">

  <!--
    The promotable dialog suite (implementation-plan §2.1, FB-12): MessageBox (M1),
    ITaskDialogService (M1), TaskDialog (M6), file dialogs (M6). Cursorial references ONLY —
    never Markdig, never the app or Document projects — so post-v1 promotion to a
    Cursorial extensions package stays a mechanical move. Enforced by ArchitectureTests.
  -->

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Cursorial.UI.Dialogs</RootNamespace>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- NuGet packaging. Shared metadata lives in ../Directory.Build.props; this carries per-package fields only. -->
  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <PackageId>Cursorial.UI.Dialogs</PackageId>
    <Version>0.4.0</Version>
    <Description>The retained-scene drawing layer for Cursorial — cached-raster scenes and a compositor that only repaints what changed, brushes and gradients, a pen/box engine with automatic box-drawing junctions, charts, and UI drawing primitives (clip/translate, occluding fills, titled panels, drop/inner shadows). Built on Cursorial.Rendering + Cursorial.Animation.</Description>
    <PackageTags>terminal;tui;drawing;scene;compositor;brush;gradient;charts;crossplatform</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Cursorial.UI.Bars\Cursorial.UI.Bars.csproj" />
    <ProjectReference Include="..\Cursorial.UI\Cursorial.UI.csproj" />
    <ProjectReference Include="..\Cursorial.UI.Xaml\Cursorial.UI.Xaml.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Cursorial.UI.Tests" />
  </ItemGroup>

</Project>
