namespace CursorialApp.Views;

// The base type comes from the generated half of the partial (the XAML root element), so
// changing the root element in CursorialView.xaml needs no edit here.
public partial class CursorialView
{
    public CursorialView() => InitializeComponent();
}
