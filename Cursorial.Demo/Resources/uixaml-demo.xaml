<DockPanel xmlns="https://cursorial.dev/ui"
           xmlns:x="https://cursorial.dev/xaml"
           Background="{StaticResource Chrome}">

    <!-- The whole window is declarative: resources, a control template, themed buttons + toggles, a
         {Binding}-driven status line, access-key mnemonics, and a ScrollViewer — loaded from this one
         embedded .xaml string at runtime by Cursorial.UI.Xaml (the P6 runtime loader). -->

    <DockPanel.Resources>
        <DemoBrush x:Key="Chrome"     Color="#1e212c"/>
        <DemoBrush x:Key="Accent"     Color="#5678dc"/>
        <DemoBrush x:Key="PanelBg"    Color="#262a3a"/>
        <DemoBrush x:Key="ButtonFace" Color="#385ca8"/>
        <DemoBrush x:Key="ControlInk" Color="#e1e6f5"/>
        <DemoBrush x:Key="CaptionInk" Color="#c8cee2"/>

        <!-- A chrome ControlTemplate whose part binds to the templated parent via {TemplateBinding}. -->
        <ControlTemplate x:Key="ChromeTemplate">
            <Border x:Name="PART_Chrome"
                    Background="{TemplateBinding Background}"
                    BorderPen="Light"/>
        </ControlTemplate>
    </DockPanel.Resources>

    <!-- Header band (docked top). -->
    <Border DockPanel.Dock="Top" Background="{StaticResource Accent}" Height="1">
        <TextBlock Text=" Cursorial.UI — Phase 6: declarative UI loaded from XAML"
                   Foreground="#f0f4ff"/>
    </Border>

    <!-- Status band (docked bottom) — its text is a {Binding} onto the demo view-model. -->
    <Border DockPanel.Dock="Bottom" Background="{StaticResource Chrome}" Height="1">
        <TextBlock x:Name="Status" Text="{Binding Status}" Foreground="{StaticResource CaptionInk}"/>
    </Border>

    <!-- The control panel, inside a scroller (LastChildFill). -->
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Border Title=" Settings " BorderPen="Light"
                Background="{StaticResource PanelBg}" Padding="1">
            <StackPanel Spacing="1" Margin="1">

                <TextBlock Text="Buttons (Tab focus, Space/Enter or Alt-mnemonic activates):"
                           Foreground="{StaticResource CaptionInk}"/>
                <StackPanel Orientation="Horizontal" Spacing="2" Margin="1,0">
                    <Button x:Name="Ok"     Content="_OK"     Background="{StaticResource ButtonFace}" Foreground="{StaticResource ControlInk}"/>
                    <Button x:Name="Cancel" Content="_Cancel" Background="{StaticResource ButtonFace}" Foreground="{StaticResource ControlInk}"/>
                    <Button x:Name="Apply"  Content="_Apply"  Background="{StaticResource ButtonFace}" Foreground="{StaticResource ControlInk}"/>
                </StackPanel>

                <TextBlock Text="Check boxes (Space / click toggles):"
                           Foreground="{StaticResource CaptionInk}"/>
                <StackPanel Margin="2,0">
                    <CheckBox x:Name="Airplane" Content=" Airplane mode" Foreground="{StaticResource ControlInk}"/>
                    <CheckBox x:Name="Wifi"     Content=" Wi-Fi"         Foreground="{StaticResource ControlInk}" IsChecked="true"/>
                </StackPanel>

                <TextBlock Text="Radio buttons (one group; arrows move + check):"
                           Foreground="{StaticResource CaptionInk}"/>
                <StackPanel Margin="2,0">
                    <RadioButton x:Name="Low"  Content=" Low"    GroupName="quality" Foreground="{StaticResource ControlInk}"/>
                    <RadioButton x:Name="Med"  Content=" Medium" GroupName="quality" Foreground="{StaticResource ControlInk}" IsChecked="true"/>
                    <RadioButton x:Name="High" Content=" High"   GroupName="quality" Foreground="{StaticResource ControlInk}"/>
                </StackPanel>

                <TextBlock Text="Bound text (the {Binding} on the view-model's Caption):"
                           Foreground="{StaticResource CaptionInk}"/>
                <TextBlock x:Name="Live" Text="{Binding Caption}" Foreground="{StaticResource Accent}" Margin="2,0"/>

                <TextBlock Text="Keys: t = cycle color tier · d = flip dark/light · Alt = access keys · q = quit"
                           Foreground="{StaticResource CaptionInk}"/>
            </StackPanel>
        </Border>
    </ScrollViewer>
</DockPanel>
